import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { FormControl, FormLabel } from "@sk-web-gui/forms";
import { Switch } from "../src";

<Meta title="Komponenter/Toggel/Översikt" />

# Toggel

<p>Använd en toggle när du vill att användaren snabbt ska kunna skifta mellan två lägen, som till exempel på och av.</p>

<p>Använd alltid en toggel tillsammans med en beskrivande text som till exempel “Av” eller “På” för tydlighet.</p>

<div className="inline-block">
    <FormControl className="flex justify-between mt-6">
        <FormLabel htmlFor="switch-on" className="mb-0 mr-2">På</FormLabel>
        <Switch defaultChecked aria-label="example switch 1" id="switch-on" />
    </FormControl>
    <FormControl className="flex justify-between mt-6">
        <FormLabel htmlFor="switch-off" className="mb-0 mr-2">Av</FormLabel>
        <Switch aria-label="example switch 2" id="switch-off" />
    </FormControl>
    <FormControl className="flex justify-between mt-6">
        <FormLabel htmlFor="switch-disabled" className="mb-0 mr-2">Inaktiv</FormLabel>
        <Switch disabled aria-label="example switch 3" id="switch-disabled" />
    </FormControl>
</div>
